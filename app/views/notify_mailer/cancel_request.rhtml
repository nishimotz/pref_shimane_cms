<%= @section.name %> 情報提供責任者様へ

ホームページ担当者からページの公開停止依頼の要求が来ています。
次のURLからページを確認し、審査を行った上で公開停止処理を行ってください。

  <%= @url %>

作成者(<%= @user_name %>・<%= @tel %>・<%= @email %>@<%= CMSConfig[:mail_domain] %>)

依頼・承認・却下日(<%= @time %>)

<% if @end_date -%>
公開期間(<%= @begin_date %> 〜 <%= @end_date %>)
<% elsif @begin_date -%>
公開期間(<%= @begin_date %> 〜)
<% else -%>
公開期間(なし)
<% end -%>

コメント(<%= @comment %>)

---
CMS 島根県ホームページ管理システム
